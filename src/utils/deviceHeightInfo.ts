/**
 * Function that returns the current height of the browser window.
 *
 * @returns {number} The current height of the browser window in pixels.
 */
const getDeviceHeightInfo = (): number => {
    return window.innerHeight;
  };
  
  export default getDeviceHeightInfo;
  